---
description: 
globs: 
alwaysApply: true
---
## The Deepdown package

Markdown templating format for generating AI-ready docs from JSON Schema, built for vector stores and RAG workflows.

Uses a Lerna monorepo to house parser and renderer logic, as well as unique spec resolvers